defmodule ChatServer do
  #   alias ChatServer.User

  @moduledoc """
  Documentation for `ChatServer`.
  """

  @doc """
  Hello world.

  ## Examples

      iex> ChatServer.hello()
      :world

  """
  def hello do
    # user_data = %User{id: "123", display_name: "Nova"}
    # IO.puts(user_data.id)
    # IO.puts(user_data.display_name)
    # encoded = User.encode(user_data)
    # IO.puts(encoded)
    :world
  end
end
